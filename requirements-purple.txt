# Slim dependency set for the Sprint 4 purple agent Docker image.
# Excludes vllm/torch/sentence-transformers/google-adk which are only needed
# by the Phase 1 green and red agents — including them bloats the image past
# the AgentBeats runner's disk limit.
a2a-sdk[http-server]>=0.3.5
openai>=2.8.1
python-dotenv>=1.1.1
uvicorn>=0.35.0
httpx>=0.24.0
fastapi>=0.100.0
pydantic>=2.11.9
